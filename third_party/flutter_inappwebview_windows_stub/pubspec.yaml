name: flutter_inappwebview_windows
description: Inert Windows implementation used when TomoRead only needs flutter_inappwebview on Android.
version: 0.6.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      windows:
        dartPluginClass: TomoReadInAppWebViewWindowsStub
